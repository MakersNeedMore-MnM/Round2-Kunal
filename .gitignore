node_modules
.next
out
dist
.vercel
.env
.env.local
.env*.local
.DS_Store
*.log

# Local agent workspace, not part of the app.
.claude/

# Throwaway QA credential the local harness hands to the browser through the
# dev server. Written by .claude/qa/qa.mjs and deleted again by its cleanup
# step; ignored here so an interrupted run can never commit one.
public/__qa.json

# Superseded copy of the whole app, kept locally only.
new raisoni/

# Report, deck, video and the screenshots they are built from — large binaries
# that are outputs of the project, not source.
deliverables/
tsconfig.tsbuildinfo
